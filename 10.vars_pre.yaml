- name: print vars prf
  hosts: local
  connection: local
  vars:
    NAME: "ansible from play"
  # vars_files:
  #   - 06.course.yaml
  vars_prompt:
    - name: NAME
      prompt: What is your username?
      private: false
  tasks:
    - name: print vars from tasks
      # vars:
      #   NAME: ansible from task
      ansible.builtin.debug:
        msg: "ansible {{ NAME  }} "


# Highest pref

  # 1. args
  # 2. Tasks
  # 3. file_fold
